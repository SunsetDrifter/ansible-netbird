---
# Playbook: Configure NetBird via API (Infrastructure as Code)
#
# Thin wrapper around the netbirdio.ansible_netbird.configure role.
# See the role documentation for full details.
#
# Preview (default — read-only diff):
#   ansible-playbook netbirdio.ansible_netbird.configure_netbird \
#     -e "configure_config_dir=/path/to/netbird_config" \
#     -e "netbird_api_url=https://netbird.example.com" \
#     -e "netbird_api_token=your-token"
#
# Apply:
#   ansible-playbook netbirdio.ansible_netbird.configure_netbird \
#     -e "configure_config_dir=/path/to/netbird_config" \
#     -e "netbird_api_url=https://netbird.example.com" \
#     -e "netbird_api_token=your-token" \
#     -e "configure_commit=true"
#
# Apply + strict:
#   ... -e "configure_commit=true" -e "configure_strict=true"
#
# Optional variables:
#   - target_hosts: Host or group to run on (default: localhost)

- name: Configure NetBird from IaC Definitions
  hosts: "{{ target_hosts | default('localhost') }}"
  gather_facts: false
  roles:
    - role: netbirdio.ansible_netbird.configure
      run_once: true
